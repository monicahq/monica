# differentiate from the deployment files
version: 2

# optional Composer settings
composer:
    # Resolves to the --no-dev parameter
    no-dev: true

# called after Composer runs
post: php artisan monica:update --force -vvv
