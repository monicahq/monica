name: Build and test

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
  release:
    types: [created]

concurrency:
  group: Build ${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    uses: monicahq/workflows/.github/workflows/tests.yml@fix-results3
    with:
      php-versions: "['8.2']"
      connections: "['sqlite', 'mysql', 'pgsql']"
      default-php-version: '8.2'
      default-connection: sqlite
      project: monica
      node-version: 18
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
