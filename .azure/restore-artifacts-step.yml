steps:
#- task: DownloadPipelineArtifact@1
#  inputs:
#    artifactName: 'vendor'
#    targetPath: $(System.DefaultWorkingDirectory)/vendor
#  displayName: 'Restore artifacts'
#- bash: |
#    sudo composer self-update
#    composer global require hirak/prestissimo
#    composer install --no-interaction --no-suggest --no-progress --ignore-platform-reqs
#  displayName: 'composer install'
- task: DownloadPipelineArtifact@1
  inputs:
    artifactName: 'build'
    targetPath: $(Build.ArtifactStagingDirectory)
  displayName: 'Restore artifacts'
- task: ExtractFiles@1
  inputs:
    archiveFilePatterns: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).tar.xz'
    destinationFolder: $(System.DefaultWorkingDirectory)
